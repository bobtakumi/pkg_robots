---
name: review-bundle
description: >
  依頼者にレビューを依頼する成果物（提案・計画・調査結果・設計文書群）ができたとき、
  または「レビューして」「レビューバンドルを作って」と指示されたときに使う。
  core/view 分離方式でレビューバンドル（core.md + views/review.md）を生成し、
  依頼者が判断点だけを短時間で判定できる状態を作る。ad-hoc なレビューガイドの
  代わりに必ずこのスキルを使うこと。
---

# review-bundle: core/view 分離レビューバンドルの生成

> **このファイルは配布複製**（プロジェクト repo の `.claude/skills/review-bundle/` に置かれている場合）。
> **正本 = dev-hub `methodology/review-bundle/`**（方針は同ディレクトリの `METHODOLOGY.md`）。
> 手順を直すときは**正本を直してから配布先へ再複製**する。ここを直接編集すると次の再配布で消える。

本書は方針（METHODOLOGY.md）の実行手順への展開。

## 手順

1. **対象確定**: レビュー待ち項目を棚卸しし、1レビューイベント = 1バンドルに統合する（乱立させない）
2. **core.md を書く**（必ず view より先）:
   - 場所: `docs/review-bundles/<date>_<topic>/core.md`
   - **同じ日に 2 枚目以降を作るときは日付に連番の英字を付ける**: `2026-08-09_<topic>` → `2026-08-09a_<topic>` → `2026-08-09b_<topic>`。ディレクトリ名を並べたときに、その日のどれが最新かが一目で分かるようにするため(日付だけだと同日分の前後が判別できない)
   - メタ: title / date / status: レビュー待ち / 対象一覧 / **as_of: 現在の HEAD ハッシュ** / changelog
   - セクション: findings / open_points / confirmations / risks
   - 全項目に 🟦(依頼者・文書・実測由来) / 🟨(自分の推測) と 確定/仮説/未確認、source_refs(path＋section)
   - open_points: 問い / 背景(なぜこの判断が発生するか) / 推奨 / 却下時(代替の実在を検証して書く) / depends_on(任意)
   - **RP は 10 個以内**。超過分は confirmations へ降格
3. **views/review.md を派生**:
   - 30秒サマリー（**箇条書き**）→「判断が必要な点は N 個」表（# | 問い | 背景 | 推奨 | 却下時）
   - **文体は淡々と**: 箇条書きを丁寧な散文にしない（短文・体言止め・飾りを削る。METHODOLOGY v2.1 §3）
   - **単独読解の原則（METHODOLOGY v2 §3）**: 「問い」列・見出し・サマリーは管理番号や内部コードネーム（RP-n・ADR-n・工事名・計測名）でなく**論点の中身を日常語で**書く。番号は括弧アンカーに留め、略号は初出で一言説明を添える。判断に必要な文脈は view に含める（view 単独でレビューが成立すること）
   - depends_on があればレビュー順を明示
   - 確認のみ / リスク要点 / レビュー後の流れ
   - 判定返却テンプレ（**先頭に「全 RP 一括 ✅」の1行**、続けて RP 別の ✅/✏️/❌＋コメント欄。**各行に内容の短い言い換えを付ける**）
4. **付随物のコミット**: 生成時の所感・改善提案・計測値（正味時間・RP数）もバンドル配下に置く
5. commit & push し、バンドルパス・RP 個数・想定レビュー時間を報告して終了

## 厳守事項

- **原本を改変しない**（参照のみ）。判定は実行しない（推奨提示まで。ゲート判定は不可侵）
- 根拠を確認できなかった記述は**未確認**とし、確信があるように書かない
- 修正依頼（✏️/❌）を受けたら **core を直してから view を再派生**。同一ディレクトリで改訂し changelog に追記
- 既存のレビューキュー（checklist 等）があれば、そこから本バンドルへの単方向リンクを1行追加するのみ。ad-hoc なレビューガイドは作らない
- 軽量原則: .md のみ。HTML 化・スクリプト化・凝った整形はしない
- **view は単独読解可能に**: 読み手が他文書や管理番号の定義を知らなくても判断できる文脈を view に含める（2026-07-10 依頼者指示・正本 = METHODOLOGY v2 §3）
